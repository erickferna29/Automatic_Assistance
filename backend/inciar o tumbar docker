-----------------------------------------------------------------------

Si no le saben asi se levanta un docker para la base de datos
docker compose up -d
esto dentro de la carpeta backend

----------------------------------------------------------------

Si no le saben asi se tumba un docker para tener la version mas actual de la base de datos del repo
docker compose down -v
esto dentro de la carpeta backend, igual

----------------------------------------------------------------